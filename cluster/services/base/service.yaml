apiVersion: v1
kind: Service
metadata:
  name: svc
  labels:
    app: echo-relay-java-app
    dep: default
spec:
  selector:
    app: echo-relay-java-app
    dep: default
  type: ClusterIP # LoadBalancer # NodePort # 
  ports:
    - protocol: TCP
      name: m-http  
      port: 80
      targetPort: 3000